version: '2'

services:
  whatsgoingon:
    build: .
    ports:
      - $EXPOSE_IP:4000:8080
    volumes:
      - .:/opt/:rw
    networks:
      - service-grp-net

networks:
  service-grp-net:
    external: true
